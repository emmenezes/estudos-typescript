let greetings: string = "OMG hiii";

// number
let userId: number = 123123;

// boolean
let isLoggedIn: boolean = false;

// Avoid explain the obvious

let hero;

function getHero() {
  return true;
}

hero = getHero();

// Avoid type 'any'

console.log(greetings);
